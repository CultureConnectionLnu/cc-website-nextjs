[build]
builder = "railpack"
buildCommand = "bash setup.sh && npm run build"

[deploy]
preDeployCommand = []
startCommand = "bash setup.sh && npm start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "never"
